fastapi
uvicorn
python-multipart
pydantic
torch
torchvision
numpy                    # image validator: array ops (also used elsewhere)
Pillow
rembg                    # image validator: background removal (pulls onnxruntime)
opencv-python-headless   # image validator: blur/blank checks + connected-components object detection
diffusers
transformers
accelerate
scipy
plyfile
trimesh
open3d
SQLAlchemy>=2.0
# supabase>=2.0  (only if PERSISTENCE_BACKEND=supabase)
