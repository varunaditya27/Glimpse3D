[pytest]
testpaths = tests
# Put the backend root (this dir) on sys.path so `import app...` works under the
# bare `pytest` console script (CI), not just `python -m pytest`.
pythonpath = .
markers =
    heavy: needs torch + ML deps
    gpu: needs CUDA
